meta {
  name: Login (User)
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test@gmail.com",
    "password": "password123"
  }
}

script:post-response {
  const body = res.getBody();
  
  if (body.accessToken) {
      bru.setEnvVar("access_token", body.accessToken);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
