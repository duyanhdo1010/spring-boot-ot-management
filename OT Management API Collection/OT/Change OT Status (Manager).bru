meta {
  name: Change OT Status (Manager)
  type: http
  seq: 2
}

patch {
  url: {{url}}/api/v1/otregistrations/5/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "otStatus": "APPROVED",
    "managerNote": "Some random note.."
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
