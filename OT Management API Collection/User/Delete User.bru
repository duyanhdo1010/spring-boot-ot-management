meta {
  name: Delete User
  type: http
  seq: 4
}

delete {
  url: {{url}}/api/v1/users/10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
