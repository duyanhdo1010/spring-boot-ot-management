meta {
  name: Create Department
  type: http
  seq: 1
}

post {
  url: {{url}}/departments/
  body: json
  auth: bearer
}

auth:bearer {
  token: 
}

body:json {
  {
      "name": "Phòng 2"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
