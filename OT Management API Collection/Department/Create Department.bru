meta {
  name: Create Department
  type: http
  seq: 1
}

post {
  url: {{url}}/departments/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "Phòng 4",
    "manager_id": 10
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
