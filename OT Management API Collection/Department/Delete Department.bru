meta {
  name: Delete Department
  type: http
  seq: 4
}

delete {
  url: {{url}}/departments/2
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
